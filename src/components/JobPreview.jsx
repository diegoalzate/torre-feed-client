import React from "react";
import withStyles from "@material-ui/core/styles/withStyles";

const style = {};

function JobPreview(props) {
  const { classes } = props;
}

export default withStyles(styles)(JobPreview);
